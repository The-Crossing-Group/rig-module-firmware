# Custom partition table for waveshare-s3-sensors, 16MB flash.
#
# Based on Espressif's stock app3M_fat9M_16MB.csv (the "16M Flash (3MB
# APP/9.9MB FATFS)" scheme this project has always built with -- see
# README.md's Arduino IDE Board Settings section), with ONE change: nvs
# grown from 0x5000 (20K) to 0x10000 (64K), the top of Espressif's own
# documented recommended NVS size range (12K-64K, see
# https://docs.espressif.com/projects/arduino-esp32/en/latest/tutorials/partition_table.html).
#
# WHY: this firmware's "rigmod" NVS namespace holds up to ~511 logical
# keys (16 sensor slots x 18 keys + 16 CAN signal slots x 13 keys + 15
# module-level keys, see config.h's saveConfig()/loadConfig() comments) --
# already at or past the old 20K partition's real capacity (~500-600
# 32-byte entries across usable pages, less once string values that don't
# fit in one entry are counted). Confirmed in the field 2026-09-10: a
# brand-new key (mbBaudSet, added months after mbBaud already existed)
# silently failed to write while the pre-existing mbBaud key next to it
# in the same saveConfig() call succeeded fine -- exactly the "partition
# full, new keys can't be created" signature this file's comments have
# warned about since v1.8.0. 64K gives ~4x the entry headroom.
#
# The 44K difference comes out of the ffat partition (9.875M -> 9.8125M,
# a 64K/0.6% shrink) -- this project only stores a small append-only
# /buffer.jsonl file there, nowhere close to needing the full 9.9M.
#
# To use: Arduino IDE auto-detects this file in the sketch folder and
# uses it INSTEAD of the "Partition Scheme" board-menu selection --
# leave that menu on "16M Flash (3MB APP/9.9MB FATFS)" anyway (cosmetic
# only once this file exists) so app0/app1 sizes still match what's
# selected.
#
# TWO THINGS THAT CAN GO WRONG, BOTH SILENT (no compile/upload error):
#
# 1. KNOWN ARDUINO-ESP32 BUG: a sketch-folder partitions.csv is sometimes
#    silently IGNORED in favor of the board-menu Partition Scheme
#    selection instead -- confirmed reports: espressif/arduino-esp32
#    issues #11579, #8502. Compiles and uploads fine either way, no
#    error -- the only way to know for sure is to check the ACTUAL
#    partition size at runtime, which is why config.h's
#    getNvsPartitionSizeBytes() exists and /system shows it. If /system
#    still shows the old 20K NVS size after flashing this, the IDE
#    ignored this file.
#
# 2. Even when this file IS used correctly, a NORMAL sketch upload does
#    NOT erase/relocate existing flash contents at the old partition
#    offsets -- it just writes the new app image + new partition TABLE.
#    The very first time this new table goes onto any given board (i.e.
#    any board that was ever flashed with the OLD 20K-nvs table before),
#    do a full flash erase first: Arduino IDE Tools menu > "Erase All
#    Flash Before Sketch Upload" > "All Flash Contents", then upload
#    once with that setting on. Switch it back to "Disabled" for normal
#    uploads afterward (leaving it on erases EVERY future upload,
#    including the buffer/config you just set up).
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x10000,
otadata,    data, ota,      0x19000,  0x2000,
app0,       app,  ota_0,    0x20000,  0x300000,
app1,       app,  ota_1,    0x320000, 0x300000,
ffat,       data, fat,      0x620000, 0x9D0000,
coredump,   data, coredump, 0xFF0000, 0x10000,
